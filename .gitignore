# Secrets — never commit these
.env
.env.*
service_account.json
*.pem
*.key

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
venv/
.venv/

# Local databases / state
*.db
*.sqlite
*.sqlite3

# Claude Code
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks*

# Local working docs (cross-session planning, design parks, audit
# scratchpads, recruiter prep, ad-hoc test plans). See the "Parked work"
# section in CLAUDE.md for what currently lives here. Anything that
# should ship as a tracked reference belongs in `docs/` instead.
notes/

# IDE / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
